@using _5school.BLL.ViewModels;
@using _5school.DAL.Enums;
@model List<ReportVM>

<div class="container">
    <div class="row">
        <div class="col-lg-6">
            <div class="card">
                <h3 class="big-text">- @SharedLocalizer["text5"] - </h3>
                
                @foreach(var item in Model.Where(r => r.ReportType == ReportType.Record && !r.IsDeleted))
                {
                    <a href="@item.FilePath" class="big-text text-decoration-none">@item.Name</a>
                }
            </div>
        </div>
        <div class="col-lg-6">
            <div class="card">
                <h3 class="big-text">- @SharedLocalizer["text6"] - </h3>

                @foreach (var item in Model.Where(r => r.ReportType == ReportType.Report && !r.IsDeleted))
                {
                    <a href="@item.FilePath" class="big-text text-decoration-none">@item.Name</a>
                }
            </div>
        </div>
    </div>
</div>